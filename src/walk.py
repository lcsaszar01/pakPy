#!/usr/bin/env python3

import os, sys 

def walk(c, freq, mii, prefix_id):
    return 0
    

def ghost_walker(g, begin_kmer_list):
    for b in begin_kmer_list:
        mii = g.find(b)
        for i in len(min.prefix):
            if(term(i)==True):
                prefix_id = i
                freq = mii.prefix_count[i]
                c=[0,0,0]
                c.append(mii.prefix[i])
                c.append(k)
                
                contigs = walk(c, freq, mii, prefix_id)
                
    
    return contigs
        
        